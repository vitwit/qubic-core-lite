
try {
    const keyHelper = require('@qubic-lib/qubic-ts-library/dist/keyHelper');
    console.log('KeyHelper exports:', keyHelper);
} catch (e) {
    console.error('Error inspecting KeyHelper:', e);
}
